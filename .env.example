# Optional app env vars live here.
# TITAN now reads persistence from the Cloudflare D1 binding declared in wrangler.jsonc.

# Auth (set via `wrangler secret put` in production; mirror in .dev.vars locally)
# AUTH_PASSWORD=replace-with-a-long-random-passphrase
# AUTH_SESSION_SECRET=replace-with-32+-bytes-of-random-base64
